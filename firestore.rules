rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ===============================================================
    // Assumed Data Model
    // ===============================================================
    //
    // Collection: users
    // Document ID: {userId}
    // Fields:
    //   - uid: string (required) - Firebase Auth UID
    //   - email: string (required) - User email
    //   - fullName: string - User full name
    //   - companyName: string - User company name
    //   - phone: string - User phone number
    //   - role: string (required) - "user" or "admin"
    //   - points: number - User points
    //   - referralCode: string - User referral code
    //   - referredBy: string - User referred by code
    //   - createdAt: timestamp - Creation date
    //
    // Collection: groupBuys
    // Document ID: {groupBuyId}
    // Fields:
    //   - id: string (required) - Group buy ID
    //   - sellerId: string (required) - Seller UID
    //   - sellerName: string - Seller name
    //   - productName: string (required) - Product name
    //   - category: string (required) - Product category
    //   - description: string - Product description
    //   - unit: string - Product unit
    //   - unitPrice: number (required) - Unit price
    //   - targetQuantity: number (required) - Target quantity
    //   - currentQuantity: number - Current quantity
    //   - images: list - Product images
    //   - status: string - "active", "completed", "cancelled"
    //   - createdAt: timestamp - Creation date
    //   - expiresAt: timestamp - Expiration date
    //
    // Collection: orders
    // Document ID: {orderId}
    // Fields:
    //   - id: string (required) - Order ID
    //   - groupBuyId: string (required) - Group buy ID
    //   - userId: string (required) - User UID
    //   - userName: string - User name
    //   - quantity: number (required) - Order quantity
    //   - totalPrice: number - Total price
    //   - status: string - "pending", "confirmed", "cancelled"
    //   - createdAt: timestamp - Creation date
    //
    // Collection: notifications
    // Document ID: {notificationId}
    // Fields:
    //   - id: string (required) - Notification ID
    //   - userId: string (required) - User UID
    //   - title: string (required) - Notification title
    //   - message: string (required) - Notification message
    //   - read: bool - Read status
    //   - createdAt: timestamp - Creation date
    //
    // ===============================================================

    // ===============================================================
    // Helper Functions
    // ===============================================================
    
    function isAuthenticated() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    function isAdmin() {
      return isAuthenticated() &&
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
          (request.auth.token.email == "ozerbaser@gmail.com" && request.auth.token.email_verified == true));
    }

    function isValidUser(data) {
      return data.keys().hasAll(['uid', 'email', 'role']) &&
             data.uid is string &&
             data.email is string &&
             data.role in ['user', 'admin'];
    }

    function isValidGroupBuy(data) {
      return data.keys().hasAll(['productName', 'category', 'unitPrice', 'targetQuantity', 'sellerId']) &&
             data.productName is string && data.productName.size() > 0 && data.productName.size() < 200 &&
             data.category is string &&
             data.unitPrice is number && data.unitPrice > 0 &&
             data.targetQuantity is number && data.targetQuantity > 0 &&
             data.sellerId is string;
    }

    function isValidOrder(data) {
      return data.keys().hasAll(['groupBuyId', 'userId', 'quantity']) &&
             data.groupBuyId is string &&
             data.userId is string &&
             data.quantity is number && data.quantity > 0;
    }

    function isValidNotification(data) {
      return data.keys().hasAll(['userId', 'title', 'message']) &&
             data.userId is string &&
             data.title is string &&
             data.message is string;
    }

    // ===============================================================
    // Rules
    // ===============================================================

    match /users/{userId} {
      allow read: if isOwner(userId) || isAdmin();
      allow create: if isAuthenticated() && isValidUser(request.resource.data) && (isOwner(userId) || isAdmin());
      allow update: if isAuthenticated() && isValidUser(request.resource.data) && (isOwner(userId) || isAdmin()) && 
                    (request.resource.data.role == resource.data.role || isAdmin());
      allow delete: if isAdmin();
    }

    match /groupBuys/{groupBuyId} {
      allow read: if true;
      allow create: if isAuthenticated() && isValidGroupBuy(request.resource.data) && (request.resource.data.sellerId == request.auth.uid || isAdmin());
      allow update: if isAuthenticated() && isValidGroupBuy(request.resource.data) && (resource.data.sellerId == request.auth.uid || isAdmin());
      allow delete: if isAuthenticated() && (resource.data.sellerId == request.auth.uid || isAdmin());
    }

    match /orders/{orderId} {
      allow read: if isAuthenticated() && (resource.data.userId == request.auth.uid || 
                   get(/databases/$(database)/documents/groupBuys/$(resource.data.groupBuyId)).data.sellerId == request.auth.uid || 
                   isAdmin());
      allow create: if isAuthenticated() && isValidOrder(request.resource.data) && (request.resource.data.userId == request.auth.uid || isAdmin());
      allow update: if isAuthenticated() && (resource.data.userId == request.auth.uid || 
                    get(/databases/$(database)/documents/groupBuys/$(resource.data.groupBuyId)).data.sellerId == request.auth.uid || 
                    isAdmin());
      allow delete: if isAdmin();
    }

    match /notifications/{notificationId} {
      allow read: if isAuthenticated() && (resource.data.userId == request.auth.uid || isAdmin());
      allow create: if isAuthenticated() && isValidNotification(request.resource.data);
      allow update: if isAuthenticated() && (resource.data.userId == request.auth.uid || isAdmin());
      allow delete: if isAuthenticated() && (resource.data.userId == request.auth.uid || isAdmin());
    }
  }
}
